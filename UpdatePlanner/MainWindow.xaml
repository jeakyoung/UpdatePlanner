<Window x:Class="UpdatePlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Update Planner" Height="310" Width="620"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="Height" Value="26"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Height" Value="28"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 경로 설정 -->
        <GroupBox Grid.Row="0" Header="경로 설정">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="업데이트 폴더:"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtSource"
                         ToolTip="하위 파일 전체를 복사할 소스 폴더 경로"/>
                <Button Grid.Row="0" Grid.Column="2" Content="찾아보기"
                        Margin="6,0,0,0" Click="BtnBrowseSource_Click"/>

                <Label Grid.Row="2" Grid.Column="0" Content="배포 위치:"/>
                <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtDest"
                         ToolTip="파일을 복사할 대상 폴더 경로"/>
                <Button Grid.Row="2" Grid.Column="2" Content="찾아보기"
                        Margin="6,0,0,0" Click="BtnBrowseDest_Click"/>
            </Grid>
        </GroupBox>

        <!-- 예약 설정 -->
        <GroupBox Grid.Row="1" Header="업데이트 예약">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Content="예약 일시:" VerticalAlignment="Center"/>
                <DatePicker Grid.Column="1" x:Name="DateSchedule"
                            VerticalAlignment="Center" Height="26"
                            ToolTip="업데이트 날짜"/>
                <Label Grid.Column="2" Content="시간:" Margin="8,0,6,0" VerticalAlignment="Center"/>

                <!-- 시간 입력 -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox x:Name="TxtHour" Text="00" Width="32" TextAlignment="Center"
                             MaxLength="2"
                             PreviewTextInput="TimeBox_PreviewTextInput"
                             LostFocus="TxtHour_LostFocus"/>
                    <TextBlock Text=":" VerticalAlignment="Center"
                               FontSize="16" FontWeight="Bold" Margin="4,0"/>
                    <TextBox x:Name="TxtMinute" Text="00" Width="32" TextAlignment="Center"
                             MaxLength="2"
                             PreviewTextInput="TimeBox_PreviewTextInput"
                             LostFocus="TxtMinute_LostFocus"/>
                </StackPanel>

                <Button Grid.Column="5" x:Name="BtnStart" Content="예약 시작"
                        Margin="8,0,0,0"
                        Background="#FF4CAF50" Foreground="White" FontWeight="Bold"
                        Click="BtnStart_Click"/>
                <Button Grid.Column="6" x:Name="BtnCancelSchedule" Content="예약 취소"
                        Margin="6,0,0,0" IsEnabled="False"
                        Click="BtnCancelSchedule_Click"/>
            </Grid>
        </GroupBox>

        <!-- 상태 -->
        <GroupBox Grid.Row="2" Header="상태">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock x:Name="TxtStatus" Text="대기 중"
                               FontSize="13" FontWeight="SemiBold" Foreground="#FF333333"/>
                    <TextBlock x:Name="TxtCountdown" Text=""
                               FontSize="12" Foreground="#FF0078D7"
                               FontFamily="Consolas" Margin="0,2,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" x:Name="BtnRunNow" Content="지금 실행"
                        VerticalAlignment="Center" Margin="0,0,6,0"
                        Click="BtnRunNow_Click"
                        ToolTip="예약 없이 즉시 복사를 실행합니다"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Label Content="로그 보관:" VerticalAlignment="Center" Padding="0"/>
                    <TextBox x:Name="TxtRetentionDays" Text="3" Width="38" Margin="4,0,0,0"
                             ToolTip="이 일수보다 오래된 로그 파일을 자동으로 삭제합니다"/>
                    <Label Content="일" VerticalAlignment="Center" Padding="4,0,0,0"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
